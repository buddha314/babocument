WebXR Integration Guide for Babylon.js Client

CURRENT CLIENT STATUS:
- Babylon.js 8.33.2 ready
- WebXRDepthSensing already imported
- Engine configured with stencil buffer (required for XR)
- Physics system active (Havok)

MAIN ENTRY POINT:
File: c:\Users\b\src\babocument\client\src\app\page.tsx
Function: handleLoad (lines 81-97)

IMPORTS NEEDED FOR WEBXR:
import { WebXRExperienceHelper } from "@babylonjs/core/XR/webXRExperienceHelper";
import { WebXRControllerPointerSelection } from "@babylonjs/core/XR/features/WebXRControllerPointerSelection";
import { WebXRMotionControllerTeleportation } from "@babylonjs/core/XR/features/WebXRMotionControllerTeleportation";
import { WebXRPlanes } from "@babylonjs/core/XR/features/WebXRPlanes";
import { WebXRDOMOverlay } from "@babylonjs/core/XR/features/WebXRDOMOverlay";
import { WebXRHandTracking } from "@babylonjs/core/XR/features/WebXRHandTracking";

SCENE FILES LOCATION ISSUE:
- Assets are in: client/assets/example.scene/
- Loader expects: public/scene/
- Solution: Copy assets to public during build or update loader path

KEY FEATURES TO IMPLEMENT:
1. WebXRExperienceHelper initialization
2. Controller pointer selection
3. Motion controller teleportation
4. Hand tracking (optional)
5. DOM overlay for UI (optional)

DEVICE COMPATIBILITY:
- Meta Quest 3, Quest Pro
- PlayStation VR2
- Microsoft HoloLens 2
- Windows Mixed Reality devices
- PC VR with WebXR support

REQUIRES:
- HTTPS connection (secure context required)
- Device XR support
- Browser WebXR support

TESTING CHECKLIST:
- Scene renders without errors
- Physics simulation works
- Camera controls work on desktop
- Script callbacks execute
- Device reports XR support
- Session request succeeds
- Controllers detected and tracked
- Touch input works
- Pointer selection works
- Teleportation works

PERFORMANCE CONSIDERATIONS:
- High precision floats already enabled
- Antialias enabled
- Need to reduce shadow quality in XR mode
- Consider disabling expensive post-processing
- Implement LOD for meshes
- Monitor frame rate for reprojection

NEXT STEPS:
1. Import WebXR classes in page.tsx
2. Initialize WebXRExperienceHelper in handleLoad
3. Setup controller features
4. Create XR interaction scripts
5. Test on target device
6. Optimize performance
7. Add hand tracking
8. Implement AR features
